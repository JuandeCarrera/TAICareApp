# Historial de Commits - TAICareApp

## 2026-02-10: Fase 1 - Herramientas y Configuración
**Mensaje del Commit:** `chore: configurar eslint, prettier, nodemon y constantes`

**Cambios:**
- **Backend:**
  - Instalado `eslint`, `prettier`, `nodemon`.
  - Configurado `.prettierrc` y `eslint.config.js`.
  - Añadidos scripts `dev`, `lint`, `format` en package.json.
  - Extraídas cadenas mágicas a `src/constants/index.js`.
  - Actualizados modelos (`User`, `Alert`, `Routine`) para usar constantes.
  - Arreglados problemas de `EADDRINUSE` usando `nodemon`.
  - **Corrección:** Permitir que los cuidadores sean dueños temporales de hogares durante la creación.
- **Frontend:**
  - Instalado `prettier`, `eslint-plugin-prettier`, `eslint-config-prettier`.
  - Configurado `.prettierrc` y `eslint.config.js`.
  - Añadido script `format` en package.json.
  - Extraídas cadenas mágicas a `src/constants/index.js` (Roles, Estados).
  - Actualizado `Register.jsx` para usar la constante `ROLES`.
  - **Corrección:** Actualizado `UsersPage.jsx` para enviar el ID del cuidador como dueño al crear un hogar rápido.

## 2026-02-10: Fase 2 - Arquitectura Backend (Refactorización)
**Mensaje del Commit:** `refactor: implementar patrón controller-service en households y manejo global de errores`

**Cambios:**
- **Backend:**
  - Creado `src/middleware/errorHandler.js` para manejo centralizado de errores.
  - Registrado `errorHandler` en `server.js`.
  - Creado `src/services/householdService.js` con toda la lógica de negocio de hogares.
  - Creado `src/controllers/householdController.js` para manejar peticiones HTTP.
  - Refactorizado `src/routes/households.js` para usar el controlador, eliminando lógica inline.

## 2026-02-10: Fase 3 - Arquitectura Frontend (React Query + Axios)
**Mensaje del Commit:** `refactorización: arquitectura frontend  - integración react-query y axios`

**Cambios:**
- **Frontend:**
  - Instalado `@tanstack/react-query` y `axios`.
  - Configurado `QueryClientProvider` en `main.jsx`.
  - Creado `src/api/axios.js` con instancia centralizada e interceptores.
  - Creados hooks personalizados: `useHouseholds` (CRUD completo) y `useUsers`.
  - Refactorizado `Hogares.jsx` eliminando `useEffect` y `fetch` manuales.
  - Refactorizado `UsersPage.jsx` para usar los nuevos hooks, mejorando la gestión de estado.

## 2026-02-10: Fase 4 - Modernización Frontend (Alertas, Rutinas, Dispositivos)
**Mensaje del Commit:** `refactor: modernizar alertas, rutinas y dispositivos con react-query`

**Cambios:**
- **Frontend:**
  - Creados hooks personalizados: `useAlerts`, `useRoutines`, `useDevices`.
  - Añadidas mutaciones `create`/`update`/`delete` a `useUsers`.
  - Refactorizado `Alertas.jsx` para eliminar `fetch` manual.
  - Refactorizado `Rutinas.jsx` con React Query y gestión de estado optimizada.
  - Refactorizado `Dispositivos.jsx` para usar los nuevos hooks.
  - Completada la refactorización de `UsersPage.jsx` eliminando código legacy.
  - Verificada la build de producción (`npm run build`).

## Feedback y Pulido
** `fix: userspage multi-hogar, darkmode inputs y wizard rutinas`

**Cambios:**
- **Frontend:**
  - `UsersPage.jsx`:
    - Ahora muestra múltiples hogares si el paciente pertenece a varios.
    - Filtrado estricto de rutinas/alertas por ID de paciente.
  - `SearchToolbar.jsx`:
    - Corregida visibilidad de `<option>` en modo oscuro (fondo sólido).
  - `Rutinas.jsx`:
    - Mejorada maquetación del Paso 3 (horarios).
    - Implementado resumen en el Paso 4 antes de guardar.

## 2026-02-12: Fase 6 - Estandarización del Backend
**Mensaje del Commit:** `refactor: patrón controller-service en users, alerts, routines, devices`

**Cambios:**
- **Backend:**
  - Completada la migración a la arquitectura Controller-Service en todos los módulos principales (`Users`, `Alerts`, `Routines`, `Devices`).
  - Creados servicios (`*Service.js`) para lógica de negocio y controladores (`*Controller.js`) para manejo de peticiones.
  - Limpieza de archivos de rutas (`routes/*.js`) delegando lógica al controlador.

## 2026-02-12: Bug Fix - Asignación de Hogares
**Mensaje del Commit:** `fix: sync user-household y filtro frontend`

**Cambios:**
- **Frontend (`UsersPage.jsx`)**: Corregido filtro para mostrar hogar si `user.household_id` coincide, aunque no esté en `household.users`.
- **Backend (`userService.js`)**: Implementada sincronización automática. Al crear/editar un usuario con `household_id`, se actualiza el array `users` del Hogar correspondiente.

## 2026-02-13: Fase 7 - Dise�o Responsive y Mejoras de UI
**Mensaje del Commit:** `feat: mobile responsiveness, sincronizaci�n sidebar y mejoras modales`

**Cambios:**
- **Global:**
  - Implementado hook `useIsMobile`.
  - Sidebar ahora funciona como overlay en m�viles (<768px).
  - Patr�n Maestro-Detalle para listas en m�vil (Usuarios, Dispositivos).
- **Correcciones UI:**
  - Sincronizaci�n del estado del men� lateral en todas las p�ginas (`Rutinas`, `Datos`, etc.).
  - A�adido prop `maxWidth` a Modales y ajustado ancho en Rutinas (800px/600px).
  - Limitada altura de modales para evitar cortes en pantallas peque�as.
  - Limpieza de c�digo (`lint`) general.
